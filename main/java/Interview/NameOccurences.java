package Interview;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.function.Function;
import java.util.stream.Collectors;
import java.util.stream.Stream;


public class NameOccurences  {




    public static void main(String[] args){

     /*  List<String> list1 = new ArrayList<>();
       list1.add("sumil");
       list1.add("sunil");
       list1.add("Anil");
       list1.add("Bali");
       list1.add("sunil");

        Map<String,Long> output= list1.stream().filter(a->a.equals("sunil")).collect(Collectors.groupingBy(Function.identity(),Collectors.counting()));
        System.out.println(output);
*/

      /*  int n =7;
        int sum = Stream.iterate(new int[]{0,1}

                tint sum = Stream.iterate(new int[]{0,1},

                        x->new int [] {x[1],x[0]+x[1]})
                        .limit(n).
                        map(x->x[0]).collect(Collectors.toList())
                        .stream()
                        .distinct()
                        .filter(i->i%2==0).mapToInt(i->i).sum();

        return sum ;*/
     /*   NameOccurences x = y->{
            return y;
        }*/

    }



}
